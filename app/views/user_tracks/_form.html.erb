<%= form_for(@user_track) do |f| %>
  <% if @user_track.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_track.errors.count, "error") %> prohibited this user_track from being saved:</h2>

      <ul>
      <% @user_track.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :track_id %><br />
    <%= f.number_field :track_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
