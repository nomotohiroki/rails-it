<%= form_for(@artist_alias) do |f| %>
  <% if @artist_alias.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artist_alias.errors.count, "error") %> prohibited this artist_alias from being saved:</h2>

      <ul>
      <% @artist_alias.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :artist_id %><br />
    <%= f.number_field :artist_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
